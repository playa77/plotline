/**
 * Placeholder test to verify the test runner works.
 * Remove or replace with real tests in subsequent work packages.
 */
import { describe, expect, it } from 'vitest';

describe('placeholder', () => {
  it('asserts basic arithmetic', () => {
    expect(1 + 1).toBe(2);
  });
});
